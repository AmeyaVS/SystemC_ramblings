## Steps

1. Use `CMake` to generate the build system files.

    ```sh
    mkdir build
    cd build
    cmake ..
    ```

2. Run the build.

    ```sh
    make
    ```

3. Run the tests:

    ```sh
    make test
    # or 
    ctest
    ```

